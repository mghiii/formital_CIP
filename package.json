{
  "name": "digital-cip",
  "private": true,
  "version": "0.1.0",
  "description": "Digital CIP - plateforme de suivi Cleaning In Place",
  "scripts": {
    "dev": "cd apps/web && ./node_modules/.bin/next dev --hostname 127.0.0.1 --port 3000",
    "build": "node scripts/validate-auth-phase.mjs",
    "test": "node --test scripts/*.test.mjs",
    "lint": "node scripts/validate-auth-phase.mjs --lint",
    "users:create": "node scripts/create-auth-users.mjs"
  },
  "packageManager": "pnpm@11.7.0",
  "workspaces": [
    "apps/*"
  ]
}
